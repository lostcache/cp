#include <bits/stdc++.h>

using namespace std;

const int inf = 1e9 + 7;
const long long ll_inf = 1e19 + 7;

void solve() {
    long long n;
    cin >> n;
    cout << 2 * n << endl;
}

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    cin >> t;
    while (t--) {
        solve();
    }
    return 0;
}
